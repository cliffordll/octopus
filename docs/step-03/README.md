# Step 03 Docs

## 说明

本目录对应 `docs/FEATURE.md` 的 `Step 3. Shared Contract 层`。

这里只放 Step 3 直接相关的共享契约文档，不再承载 Step 4 之后的数据库、workflow、runtime 或 chat 草案。

## 当前文件

- `API-COMPATIBILITY.md`
  - 第一批 HTTP 契约盘点
- `SCHEMA-COMPATIBILITY.md`
  - 第一批数据库表与字段盘点
- `SHARED-CONTRACT-DRAFT.md`
  - Step 3 的 shared contract 草案

## 使用规则

- Step 1、Step 2 的执行记录不再放入本目录
- Step 4 到 Step 10 的约束文档分别放入各自的 `docs/step-04/` 到 `docs/step-10/`
- 本目录只保留 Step 3 需要的 API、schema、shared contract 约束
