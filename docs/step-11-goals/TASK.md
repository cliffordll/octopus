# Step 11: Goal 管理

状态：待开发

## 任务

- 对照上游证据实现 goal contract、schema/query 与 API。
- 支持 goal 与 organization/project 的兼容关联行为。
- 固定上游已有的 goal 状态值和更新语义。

## 不包含

- Agent 或 runtime 执行。

## 验收

- Tests 覆盖关系校验、状态值和跨 organization 拒绝行为。
