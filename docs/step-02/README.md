# Step 02 Docs

## 说明

本目录只对应 `docs/FEATURE.md` 的 `Step 2. 服务端骨架落地`。

这里存放的是 Step 2 的骨架实现记录、启动链路说明和边界约束，不承载共享契约清单本体。

## 当前文件

- `SERVER-SKELETON.md`
  - Step 2 的服务端骨架执行记录

## 与其它步骤的边界

- Step 1 的基础收敛记录放在 `docs/step-01/`
- Step 3 的共享契约与后续兼容约束统一放在 `docs/step-03/`
