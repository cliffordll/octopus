# Step 01 Docs

## 说明

本目录只对应 `docs/FEATURE.md` 的 `Step 1. 仓库基础收敛`。

这里存放的是 Step 1 的实际执行记录、取舍依据和验收材料，不承载后续共享契约或 workflow 约束。

## 当前文件

- `BASELINE-CLEANUP.md`
  - Step 1 的基础收敛执行记录

## 与其它步骤的边界

- Step 2 的服务端骨架记录放在 `docs/step-02/`
- Step 3 以及后续步骤依赖的共享契约、schema 盘点、workflow 约束统一放在 `docs/step-03/`
