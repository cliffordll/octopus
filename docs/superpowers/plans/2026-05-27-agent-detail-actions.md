# 智能体详情操作入口实施计划

1. 为智能体详情页和新对话页添加前端行为测试，覆盖顶部操作区、任务弹窗创建和 URL 预选智能体。
2. 扩展前端任务创建类型，将当前智能体 id 作为 `assigneeAgentId` 提交。
3. 在智能体详情页实现顶部操作区、分配任务弹窗以及聊天跳转。
4. 在新对话页读取 `agentId` 查询参数，并只对可聊天智能体执行预选。
5. 运行 UI 测试、类型检查和构建，并确认改动未触及服务端目录。
